/* demo3grelos — cleartext argument to window.eval (after hex-table unpack).
 * Packing style: Magento-era var _0x…=["\x.."] → eval (same family as demo1shop).
 * Payload: Grelos-named marker only — not a full published skimmer body.
 */
var grelos_v = { snd: null, Glink: 'https://example.invalid/research-only', myid: 'demo' };
